# Deployment notes

This Vite app is ready to deploy to Vercel.

## Deploy

1. Push the project to GitHub.
2. Import the repo in Vercel.
3. Use the default Vite build settings.
4. Ensure the `dist` directory is used for output.

The included `vercel.json` handles SPA routing for the app.
